package Game.Entities;

import inf.elte.hu.gameengine_javafx.Core.Architecture.Entity;

public class OnboardingEntity extends Entity {
    public OnboardingEntity() {
        //Add new components, access position and parent components to update them, add the entity to the manager using addToManager.
    }
}
